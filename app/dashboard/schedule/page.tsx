import{requireProfile}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";export default async function Page(){const p=await requireProfile();const s=await createClient();const{data=[]}=await s.from("event_schedule").select("*").eq("organization_id",p.organization_id).order("start_time");return <><h1>Schedule</h1><div className="timeline">{data.map(x=><div className="timeline-item" key={x.id}><strong>{x.title}</strong><div className="muted">{new Date(x.start_time).toLocaleString()} · {x.location}</div><div>{x.description}</div></div>)}</div></>}